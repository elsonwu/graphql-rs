//! REST API endpoints for health checks and schema management
//!
//! This module will contain REST endpoints for non-GraphQL operations.
//! Implementation will be done in later PRs.

/// Placeholder for REST handlers
pub struct RestHandler;

impl RestHandler {
    /// Create a new REST handler (placeholder)
    pub fn new() -> Self {
        Self
    }
}

impl Default for RestHandler {
    fn default() -> Self {
        Self::new()
    }
}
