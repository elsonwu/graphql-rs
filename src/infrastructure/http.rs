//! HTTP infrastructure for GraphQL server
//!
//! This module will contain HTTP handlers, middleware, and server setup.
//! Implementation will be done in later PRs.

/// Placeholder for HTTP infrastructure
pub struct HttpServer;

impl HttpServer {
    /// Create a new HTTP server (placeholder)
    pub fn new() -> Self {
        Self
    }
}

impl Default for HttpServer {
    fn default() -> Self {
        Self::new()
    }
}
